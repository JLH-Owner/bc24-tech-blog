<h2>Recent Blog Posts</h2>
{{#each posts}}
    <div class="post">
        <h3>{{this.title}}</h3>
        <p>{{this.content}}</p>
        <form action="/posts/{{this.id}}/comments" method="POST">
            <button type="submit">Submit</button>
        </form>
    </div>
{{/each}}